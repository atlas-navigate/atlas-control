# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
venv/
.venv/

# ── Runtime data (generated at install / runtime, never committed) ────────────
data/atlas.db
data/meshtastic.db*
data/.secret_key
data/wifi_hints.json
atlas.db
logs/
*.log

# ── Local datasets (downloaded by download_cities.py / scratch) ───────────────
data/US.txt
data/US_zips.zip
data/cities5000.*

# ── Local working files (not part of the app) ─────────────────────────────────
UPS_Power_Module_C/
UPS_Power_Module_C.zip
Atlas Control logo.png
remove app icons.png
setupwizard*.png

# ── Map data (large downloads — populated by install.sh) ─────────────────────
static/tiles/
static/fonts/
static/data/

# ── OSRM routing data (100+ GB, generated by install.sh) ─────────────────────
osrm-data/
osrm_active.json

# ── PMTiles CLI binary (ARM64, downloaded by install.sh) ─────────────────────
pmtiles

# ── SSL / secrets ─────────────────────────────────────────────────────────────
cert.pem
key.pem
hotspot_config.json
*.pem
*.key

# ── Temp / build artifacts ────────────────────────────────────────────────────
*.tmp
*.swp
*.swo
.DS_Store

# ── Claude Code session files ─────────────────────────────────────────────────
.claude/

# ── OSRM lib symlink (points to system path, not portable) ───────────────────
lib
